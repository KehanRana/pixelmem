# Copy to `frontend/.env.local` — Next.js auto-loads this on `npm run dev`.
# Not used in production (Railway uses dashboard variables; Docker uses build args).

PIXELMEM_BACKEND_URL=http://localhost:8000
